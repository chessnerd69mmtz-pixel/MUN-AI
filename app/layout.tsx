import './globals.css';
export const metadata = { title: 'MUN AI', description: 'Research, strategy and resolution drafting for MUN delegates' };
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
